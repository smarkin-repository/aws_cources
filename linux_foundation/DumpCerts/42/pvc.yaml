apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv-volume1-claim
spec:
  storageClassName: csi-hostpath-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
