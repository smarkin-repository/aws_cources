apiVersion: v1
kind: Pod
metadata:
    annotations: 
        description: "simple pod for linux foundation" 
    name: nginx
spec:
    containers:
    - name: nginx
      image: nginx
      ports:
      - containerPort: 80
